# Supabase Configuration
# These are required for the application to work

# Public anon key - exposed to the browser 
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Service role key - only used on the server, DO NOT expose to the browser
# If provided, this will be used for server-side API requests instead of the anon key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Note: Copy this file to .env.local and update the values
# Never commit .env.local to version control